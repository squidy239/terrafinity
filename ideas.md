1. Biome methods: climate fields + erosion-proxy moisture into per-column biome id driving randGround thresholds and per-biome tree sets; see `Biome methods` §5+recommendation. Links: https://www.researchgate.net/figure/Whittaker-Biome-Diagram-derivation-Whittaker-1975_fig3_330811543, https://github.com/luanti-org/luanti/blob/master/doc/lua_api.md.
2. Light bricks vs CSM sun: hybrid verdict — CSM for sun direct visibility, bricks for blocklights/ambient/bounce; see `Light bricks vs CSM sun`.
3. Mesh renderer (mesh_shaders.md; face packing do-first, water scroll do-next): task+mesh as the single pipeline, 16-bit header-deduped faces, procedural dynamic voxels (water displacement, grass/cloud seeds); drops Apple/MoltenVK.
4. Sunlight underwater: virtual entry term first, then unshadowed analytic in-scatter + depth transmittance in the composite; never shadow the volume; see `Sunlight underwater`.
## Biome methods (deep)

| # | Method | How it picks a biome | Fit for this pipeline | Cost |
| - | ------ | -------------------- | --------------------- | ---- |
| 1 | Whittaker 2-axis polygon lookup | Two low-freq fields (temperature, precipitation) sampled per column; point-in-polygon over ~9 Whittaker polygons (tundra/taiga/grassland/desert/forest/rainforest splits) yields a biome id | Best conceptual match: fields live in normalized height-pipeline space next to large_terrain_noise, thread through GroundContext into randGround cover thresholds (snow_line, ground_threshold, dirt_band become per-biome); altitude + slope stay as post-filters (snow_cliff_slope, sea_floor_rock_slope) so existing rock/sand rules survive | 2 extra 2D noise samples/column (same lattice/interp as continental noise); one polygon lookup; LOD-safe if fields use large_terrain_interp-style coarse lattice |
| 2 | Luanti/Voronoi heat+humidity points | Each biome registers a (heat_point, humidity_point); per-column noise values snap to nearest registered point in climate space (Voronoi cells), optional weight + blend noise to soften borders | Closest to per-biome trees[]/JitteredGrid sets: biome id selects which TreeConfig entries are enabled per column, reusing existing placer grids at different box_size; Params declaration-default compat holds if biome table is a fixed-size array with defaults (like trees[] is today) surfaced via generator_api C-ABI config tree | Same 2 field samples as (1) + nearest-point over N biomes (N small); border blend costs one more noise sample only if enabled |
| 3 | Minecraft 5-param multi-noise nearest point | 5 noise params per 4x4x4 cell (temperature, humidity, continentalness, erosion, weirdness); each biome owns a target box/point, nearest match wins; noise router couples biome choice to density so geology stays consistent | Overkill but instructive: continentalness already exists here as large_terrain_noise, erosion proxy already exists as the Phacelle filter output; adopting only the "nearest target point in N-D climate space" matcher (instead of hand thresholds) makes adding biomes data-driven without touching randGround branching | 5 field samples vs 2; matcher cost scales with biome count x dims; 3D sampling complicates LOD/interp lattices (cave_interp-style grids help); only worth it past ~8 biomes |
| 4 | Dwarf Fortress layered thresholds | Fixed evaluation order: elevation band (ocean/land/mountain) -> temperature (latitude + altitude lapse) -> rainfall x drainage table to ecosystem | Maps 1:1 onto randGround's existing order (sea_level/beach -> snow_line/cover lerp -> dirt_band/stone); drainage approximated by slope + height-above-sea already in GroundContext (sea_floor_rock_slope, sand_slope_falloff); cheapest path: keep randGround branching, just drive its thresholds from (temp, rain) fields instead of constants | ~0 new infra: 2 fields + threshold table; deterministic across LODs; risk is blocky bands unless slope_jitter/cover_rand dithering (already in randGround) is kept |
| 5 | Climate-field + erosion-proxy moisture (recommended) | Temperature = low-freq noise minus altitude lapse (height_norm term already in randGround); moisture = second low-freq noise modulated by erosion-filter output (gullies/valleys read wetter) and concavity approx from getDifferential; no true hydrology | Designed for this codebase: reuses HeightCtx/erosionFilterParams per-LOD octave dropping, grassSlopes differential, and GroundContext threading; moisture needs no new simulation, just a weighted mix of existing signals; per-biome tree/cover sets key off the resulting id | 2 low-freq samples + reuse of already-computed erosion/slope values; LOD-consistent by construction (same octave-drop rule); slight coupling: moisture shifts when erosion params change |
| 6 | Full hydrology (TWI/flow accumulation/particle erosion) | Precipitation field + downhill flow (D8/MFD) or particle erosion pass produces accumulation/a map; TWI = ln(a/tan beta) becomes the moisture axis | Worst fit here: needs cross-chunk watershed state, breaks the embarrassingly-parallel per-chunk genChunk + terrain_height_cache design and LOD interp lattices; particle carving fights the analytic Phacelle filter already tuned in erosion.zig | Global passes + neighbor dependencies + seed-stable sink-filling; only revisit if rivers/lakes become explicit features, and then as a coarse logic-grid overlay, not per-voxel sim |
| 7 | Per-biome decoration sets (structure layer, not a picker) | Orthogonal to pickers above: biome id gates which JitteredGrid placers run (TreeConfig per biome: placer box_size/inner_box_size, size_variation, Tree.Config), plus cover-block overrides in randGround | Already half-built: trees[] exists with per-entry placer grids; work is threading biome id into genStructures + randGround and exposing one array entry per biome in Params/generator_api (hidden Tree.Config stays unreflectable as today) | Negligible runtime (skipped placers cost nothing); config-tree cost is UI surface per biome; keep biome count small so Params defaults + presets stay manageable |

Recommendation: implement 5 (fields + erosion-proxy moisture) with 1's Whittaker polygons as the lookup and 2's Voronoi snap as the upgrade path when biome points become user-registered; keep 4's evaluation order inside randGround; use 7 for trees/cover per biome. Defer 6. New Params fields carry declaration defaults (pub const default = Params{} pattern) so old ZON configs parse, and surface through generator_api fromStruct/fromTree + C-ABI config tree like terrain_noise/trees do today.

Links: https://www.researchgate.net/figure/Whittakers-Biome-Diagram-Whittakers-scheme-uses-climatologies-of-precipitation-and_fig2_387834540, https://api.luanti.org/definition-tables/, https://rubenwardy.com/minetest_modding_book/en/advmap/biomesdeco.html, https://www.alanzucconi.com/2022/06/05/minecraft-world-generation/, https://dwarffortresswiki.org/index.php/Biome, https://github.com/weigert/SimpleHydrology, https://nickmcd.me/2020/04/15/procedural-hydrology/
## Light bricks vs CSM sun (deep)
Grounding: the brick proposal (`src/Renderer/vulkan/vulkan_renderer_improvements.md`) keeps the **sun as an unchanged directional `sun_day` term with no CSM** and adds CPU-baked sparse chunk-local 3D light bricks (ChunkSize=32, sample spacing 4, phase 2, `brick_size=11`, 11^3 RGBA16F ~10.6 KB per chunk-layer, one 3D-texture layer per chunk, sampled once per fragment). The current CSM (`src/Renderer/vulkan/shadow/Csm.zig` + `ShadowRenderer.zig`) is a 12-cascade PSSM (`min_split_radius=4` to `max_shadow_distance=128000`, `pssm_lambda=1`, 4096 maps) with texel snapping (`snapCenter`), concentric camera-centered spheres (rotation-invariant), staleness padding from measured `per_frame_dist`, latched light basis stepped in ~1-texel increments (`shouldStepLight`/`latchLight`), and a refresh scheduler (`refreshIntervals` 64->512 frames, `nextRefreshSet`, `cascades_per_frame=1` budget, pending/promote sampling discipline). Question: could RTXGI/DDGI-style irradiance bricks REPLACE the CSM for the sun?
| # | Angle | Light bricks for sun | CSM sun (status quo) | Winner |
|---|-------|----------------------|----------------------|--------|
| 1 | Signal fit: directional occlusion vs diffuse cache | Bricks store low-frequency irradiance; DDGI/RTXGI literature is explicit that probes are a diffuse indirect signal and do NOT replace direct-light shadows (sharp sun contact shadows, tree/canopy dapple, cliff-line definition all live above probe Nyquist). A 4-block brick spacing cannot resolve a 1-block wall shadow or a cave mouth; trilinear sampling smears the terminator. | CSM stores exactly the sun signal: per-texel binary visibility along parallel rays, PCF-blurred in world units (`blur_radius=1.0` converted per cascade) with cascade cross-fade (`cascade_blend`) hiding seams. | CSM, decisively. Bricks answer a different question (what light arrives) than shadows ask (is the sun visible). |
| 2 | Leaking at voxel scale | Brick spacing 4 with phase-2 halo means any occluder thinner than ~4 blocks (1-block walls, doors, leaves, fences) falls between samples or averages into the halo; the classic SVO/voxel-GI failure (thin geometry leaks, mip-averaging turns walls semi-transparent). DDGI fixes this only with per-probe depth + Chebyshev visibility tests driven by ray tracing, which the CPU-baked brick design has no equivalent of. | CSM leaks are bounded and tunable: depth/normal bias (`depth_bias_*`, `normal_bias_scale=1.5`) trade acne vs peter-panning per cascade, verified by matrix ground-truth tests (`depth range maps near/far to Vulkan depth`, `rebuilt matrix at new origin`). | CSM. Bricks without a visibility channel leak strictly worse at 1-block voxel scale. |
| 3 | Planet-scale placement and memory | Sun shadows must cover the 128 km LOD band everywhere the camera looks, including far coarse chunks. Bricks are chunk-local layers: every resident chunk pays ~10.6 KB even where the sun is the only light, and empty air chunks still need layers to answer "lit?" at their surfaces. Scaling brick density up to sun-shadow sharpness (sub-block spacing) multiplies memory 64x per halving and reintroduces the dense-grid cost sparsity was meant to avoid. | 12x4096 D32 cascade array is large (~768 MB worst case) but resolution concentrates where it matters via PSSM log distribution, and far cascades refresh rarely (up to 512 frames) so frame cost stays flat at ~1 raster/frame. | CSM at planet scale; bricks win only as a sparse local-light cache (their designed role), not as global sun coverage. |
| 4 | Update rate under sun motion and edits | Sun moves continuously: every brick containing a shadow boundary invalidates every few seconds, and unlike a torch edit (local re-bake of 1 chunk + neighbors) a sun step dirties the whole resident set at once. CPU re-bake of thousands of 11^3 bricks per sun step dwarfs the flood-fill cost bricks were chosen for, and temporal lag (DDGI's known world-space lag on lighting change) visibly drags sun shadows behind time-of-day. | CSM was built for exactly this: latched basis holds the texel grid frozen while the sun drifts (no swimming), steps discretely (~1 near-texel), staleness padding absorbs camera motion between refreshes, and the scheduler spends 1 raster/frame on the most-overdue cascade. Moving sun = free (basis relatch), moving camera = bounded (padding + near-cascade priority). | CSM, by design. Brick invalidation is local-edit-friendly but global-sun-hostile. |
| 5 | Bake/ray cost and fallback hardware | True RTXGI-style bricks need per-probe ray casts (1-2 Mrays/frame class on RTX hardware) plus BVH upkeep; the CPU-baked flood-fill brick avoids rays but then has no visibility term (see angle 2). Either way there is no Apple/MoltenVK-friendly path without a second implementation, whereas depth rasterization runs everywhere including the required vertex-path fallback. | CSM is plain depth rasterization of the existing face buffers through the shadow pipeline; no ray tracing, no new hardware gate, works on every target including MoltenVK. Graceful degradation is already implemented (contiguous-valid-prefix `cascade_count` early-out, `last_cascade_fade` to lit at the map edge, sun-gated disable at night). | CSM on cost and portability. |
| 6 | Quality scaling and failure modes | Brick sun failure is silent and global: wrong everywhere (leaked interiors, glowing caves, missing noon contrast) with no per-pixel fallback. Raising quality means denser bricks everywhere, paid even in empty terrain. | CSM failure is local and diagnosable: acne/peter-pan/seam in one cascade band, fixable with bias/blur/blend knobs and covered by unit tests (snapping exactness, split monotonicity, refresh budget/due-gating, staleness monotonicity). Resolution follows the PSSM ramp, not a uniform grid. | CSM: its artifacts are tunable parameters, bricks' are structural. |
| 7 | What bricks are actually good for (hybrid) | Bricks excel at exactly what the proposal assigns them: sparse local/block lights (torch in a cave bends around corners, zero per-frame cost, one fragment sample, flood-fill re-bake on edit). They also suit sky/ambient/bounce approximation where low frequency is correct. DDGI itself ships as direct-light-shadow-maps PLUS probes, never probes alone. | CSM cannot do bounce, ambient occlusion in caves, or colored blocklight; it answers sun visibility and nothing else. | Hybrid wins: keep both, each on its native signal. |
Recommendation / verdict: WORSE as a replacement, BETTER as a complement. Do not replace the CSM sun with light bricks under any near-term condition: bricks lose on signal fit, 1-block leaking, planet-scale coverage, sun-motion invalidation, hardware cost, and failure modes (angles 1-6). The proposal file already reaches this conclusion (`Sun: unchanged ... No CSM` in the sense of no shadow maps at all is the acknowledged weakness, bricks cover blocklights only). Ship the hybrid: CSM for sun direct visibility (keep the snapping/scheduler/staleness machinery), bricks for blocklights + later sky/ambient/bounce. Revisit replacement only if ALL of these hold simultaneously: (a) bricks gain a visibility channel (per-texel depth/direction + Chebyshev-style test, i.e. real DDGI probes, not baked irradiance), (b) ray-tracing or GPU cone-trace budget exists for probe updates on all target hardware incl. Apple fallback, (c) brick spacing reaches ~1 block near the camera (clipmap/cascade of bricks mirroring the PSSM ramp) without blowing the ~10.6 KB/chunk budget, and (d) the CSM's VRAM (12x4096 array) profiles as the actual bottleneck. Until then, bricks replacing CSM means trading crisp sun shadows everywhere for soft leaked light everywhere.
Links: https://morgan3d.github.io/articles/2019-04-01-ddgi/, https://github.com/NVIDIAGameWorks/RTXGI-DDGI/blob/main/docs/Algorithms.md, https://developer.nvidia.com/blog/an-engineers-guide-to-integrating-ddgi/, https://bugnet.io/blog/how-to-fix-voxelgi-light-leaking-and-blocky-bounce-godot, https://docs.godotengine.org/en/latest/tutorials/3d/global_illumination/using_voxel_gi.html, https://therealmjp.github.io/posts/shadow-maps/, http://longforgottenblog.blogspot.com/2014/12/rendering-post-stable-cascaded-shadow.html
## Sunlight underwater (deep)

Grounding: water volume lives in transparent_frag.frag as per-face optical depth `td = density * (bg - frag) * slant * sign`, accumulated into accum/volume_weight and resolved in composite_frag.frag as `transmission = exp(-optical_depth)` plus a scatter term `avg_volume_color * scatter_light * (1 - transmission)`. Three invariants bind every method below: (a) absorption is position-independent (`1 - volume_color`, no light term) so entry/exit face pairs cancel exactly; day/night enters only via `OitCompositor.volumeScatterLight` in the composite; the surface term alone may call `sampleShadow` (shadow/shadow.glsl, 16-tap golden-spiral PCF per cascade). (b) Sky pixels hold cleared depth 0.0 (= infinite), so `sky_dist` (256) substitutes the reference for sky only; real backgrounds keep true distance to planet scale, with `sky_dist_max` (2048) clamping sky-referenced distances symmetrically; slant `length(frag_pos)/fragment_depth` converts eye-Z to ray length. (c) Known gap: camera inside water has an exit face but no entry face, so net td goes negative and clamps to zero = no underwater tint; the fix needs a virtual entry term in the composite or raymarched volumes. Question asked: viewed from UNDER water, can the SUN carry distance and light in the volume, and what if it casts LIGHT (forward-scattered in-scatter, no shadow lookup) instead of shadows?

| # | Method | How it works | Fit for this pipeline | Cost / risk |
| - | ------ | ------------ | --------------------- | ---- |
| 1 | Status quo: shadowed sun on the surface term only | transparent_frag.frag already does `light = mix(ambient) + ndl * sun_day * sampleShadow(frag_pos)` for front-facing water faces; the volume absorption stays unshadowed and the composite multiplies a day/night scalar. Sun shades the water skin, never the water body | Baseline, zero risk, and the only method that already respects pair cancellation. But underwater it reads wrong: submerged rock/floor shading gets CSM shadows while the water column between eye and rock gets none, so depth reads flat and looking up at the sun disc shows no brightening | Free; insufficient alone — distance cue comes only from surface fresnel + Beer-Lambert-ish absorption, no directional light in the medium |
| 2 | Analytic sun in-scatter, unshadowed (recommended light term; sun casts LIGHT not shadows) | Add to the composite scatter term a forward-scattering lobe evaluated per pixel from already-available varyings: `phase = HG(dot(view_dir, -sun_dir), g)` with g ~0.6-0.85 for water particulates (or cheap Schlick `phase ~ (1-g^2)/(1+g*cos)^2` avoiding the pow), times `sun_day * sun_color * (1 - transmission)`. No shadow-map lookup, no per-face light: position-independent by construction, so entry/exit pairs still cancel exactly | Best fit for the invariant: it lands in composite_frag.frag next to `volumeScatterLight`, needs only sun_dir (already a push/uniform) + reconstructed view ray, costs ~10 ALU, works at any distance including planet scale, and degrades gracefully at night (sun_day -> 0 leaves the ambient scatter). Gives the "look toward the sun = brighter water" cue that sells being underwater | Loses occlusion: sun glows through rock walls/overhangs since there is no shadow term; mitigate with cheap proxies (method 4 depth fade, method 6 caustics masked by ndl) rather than a real lookup. Single-HG misses seawater backscatter lobe; upgrade to double-HG later for ~2x ALU |
| 3 | Shadowed sun inside the volume integral (sampleShadow per face or per pixel in composite) | Multiply the volume/scatter term by `sampleShadow(...)`: either per transparent face in transparent_frag.frag (like the surface term) or once per pixel in the composite using reconstructed world pos | Rejected for the volume: breaks invariant (a). Entry and exit faces sample light at different points (different cascades/PCF taps), so the pair no longer cancels and leaves a shadow-shaped residue even for constant-thickness water; per-pixel composite sampling is better (one lookup per pixel) but still needs world-pos reconstruction the composite does not have today, plus 16 PCF taps in the fullscreen pass. CSM cascades are also tuned for above-water texel density and fade to lit (`fade_start/fade_end`) at distance, so deep/far water silently unshadows | Highest cost (fullscreen PCF), correctness hazard (residue, cascade fade), fights the f16 accum precision; keep CSM on surface/opaque terms only |
| 4 | Depth-from-sun transmittance: Beer-Lambert downwelling by water depth | Attenuate the in-scatter/sun term by the fragment's depth below the surface: `sun_through_water = sun_color * exp(-Kd_rgb * depth_below_surface / max(sun_dir.y, eps))`, with per-channel Kd (red >> green > blue; Jerlov water types / diffuse downwelling Kd as the physically grounded table, Monzon et al. 2024). Depth source: water-surface depth vs opaque/scene depth already in the pipeline (opaque_depth_texture), or a world-y surface level uniform for flat seas | Strong fit as the distance cue the question asks for: sun gives BOTH light (method 2 lobe) and distance (this exponential). Position-safe if evaluated in the composite from per-pixel depth (one value per pixel, not per face, so pairs still cancel). Spectral Kd gives the red-first die-off essentially free (one exp3) and makes shallow vs deep reads instantly | Needs a robust depth-below-surface per pixel (surface depth texture or ray-plane for camera-in-volume); grazing sun (`sun_dir.y -> 0`) needs a clamp to avoid infinite path; Kd table is new uniforms but tiny (3 floats or a 1D LUT) |
| 5 | Height-fog / water-fog screen-space models (exponential height fog, UE-style volumetric fog grid, radial-blur god rays) | Classic alternatives to face-pair integration: (a) exp height fog `f = 1 - exp(-density * height_falloff * dist)` keyed to camera height relative to water level; (b) froxel/volumetric-fog 3D grid with in-scatter + shadow sampling per cell; (c) Mitchell GPU-Gems-3 screen-space radial blur around the sun for shafts | (a) is the cheapest underwater-tint fallback and composes with the existing composite (a fullscreen tint when camera is submerged), but it double-counts the face-pair volume unless the face-pair term is disabled for the in-volume case (see method 7). (b) is the graduation path to real shafts-with-shadows but is a second volume system alongside OIT accum/reveal (memory + maintenance). (c) is rejected underwater: sun is rarely visible through turbid water and the blur has no depth cue | (a): ~zero cost, do as part of the virtual-entry fix. (b): large (3D textures, temporal reprojection). (c): cheap but wrong look underwater |
| 6 | Caustics as cheap sun-detail (not sun-light) | Animated light detail on submerged opaque surfaces: (a) two scrolling tileable caustic textures, world-xz projected, masked by `ndl * sun_day * exp(-Kd*depth)`; (b) procedural Voronoi/worley interference (~3-5 octaves) same projection; (c) ddx/ddy-of-normal analytic convergence from the water surface waves | Orthogonal complement to method 2: in-scatter lights the water body, caustics texture the floor. (a) is cheapest (2 texture taps in fragshader.frag gated by a below-water flag) and art-directable; mask by depth fade so caustics die with depth exactly as method 4 dictates. Needs a "fragment is underwater" test (water-level uniform vs world y, or stencil from water faces) which the opaque pass lacks today | (a): trivial ALU + texture memory, do second. (b): ALU-heavy at full screen, keep as fallback if no texture budget. Never drive caustics from a shadow lookup; they ARE the fake for shadowed light |
| 7 | Face-pair integration fixes: virtual entry term vs raymarched volumes | The underwater-tint gap is structural: inside water there is no entry face. Options: (a) virtual entry in the composite — when camera is submerged (scatter_enabled flag path already exists), synthesize `td_entry = density * absorption * dist entrée` from camera pos to the exit face (or to bg), so the pair completes; (b) fullscreen water-depth reconstruction (separate water-surface depth prepass, opaque depth already sampled) giving per-pixel in-water path length directly; (c) full raymarched volume (march the OIT targets or a separate pass) | (a) is the minimal unblocker: one branch in the composite reusing existing td/accum plumbing, keeps f16 writes small (entry is analytic, not a saturated face write), and every sun method above depends on it — without it methods 2/4 light a transmission of ~1 (nothing to scatter against). (b) is the robust form of (a) for non-flat surfaces (waves, overhangs). (c) is overkill while geometry is voxel faces: marching duplicates the face-pair integral at 10-100x cost | (a): small, do first. (b): one extra depth target or reuse of opaque depth + surface plane math. (c): deferred until volumes decouple from faces |
| 8 | Underwater ambient / multiple-scattering floor (diffuse attenuation ambient, Jerlov ambient tint) | Replace/augment the flat `mix(ambient_min, ambient_max, sun_day)` with a depth-tinted veiling light: `ambient_water(depth) = water_tint * exp(-Kd*depth) + deep_blue * (1 - exp(-Kd*depth))`, i.e. shallow keeps sun-tinted scatter, deep asymptotes to dark blue independent of view direction. This is the multiple-scattering residue that single-scatter HG (method 2) cannot produce | Cheap, composite-only, position-safe (function of per-pixel depth, not per-face light). Carries the "deep = dark + blue" cue even when the sun lobe is off (night, looking away from sun). Pairs with method 4 sharing the same Kd uniforms | Pure heuristic without a phase function; keep subtle so it never fights method 2's directional term; tune against Jerlov Type I-III curves rather than hand-picked colors |

Recommendation: in order — (1) fix the virtual entry term (method 7a; without it no underwater sun method has a volume to light). (2) Add unshadowed analytic in-scatter + depth-from-sun transmittance in the composite (methods 2+4 sharing one Kd uniform triple): `scatter += sun_color * HG(cos_view_sun) * exp(-Kd * depth/sun_elev) * sun_day * (1 - transmission)` — zero shadow lookups, zero per-face light terms, pair cancellation intact. (3) Add world-projected animated caustics on submerged opaques masked by the same depth/sun terms (method 6a). (4) Add the depth-tinted ambient floor (method 8). Explicitly do NOT shadow the volume (method 3), do NOT build a froxel grid or screen-space god-ray blur yet (method 5b/c), do NOT raymarch (method 7c) — re-evaluate only when caves/overhangs make unshadowed in-scatter visibly leak through rock, at which point the fix is a single composite shadow tap at reconstructed pos, never per-face shadowed absorption.
Links: https://developer.nvidia.com/gpugems/gpugems/part-i-natural-effects/chapter-2-rendering-water-caustics, https://developer.nvidia.com/gpugems/gpugems3/part-ii-light-and-shadows/chapter-13-volumetric-light-scattering-post-process, https://graphics.unizar.es/projects/EG24Underwater/, https://www.oceanopticsbook.info/view/scattering/level-2/the-henyey-greenstein-phase-function, https://en.wikipedia.org/wiki/Henyey%E2%80%93Greenstein_phase_function, https://www.pbr-book.org/4ed/Volume_Scattering/Phase_Functions, https://research.nvidia.com/labs/rtr/approximate-mie/, http://www.cs.columbia.edu/~bosun/sig05.htm, https://cseweb.ucsd.edu/~ravir/papers/singlescat/scattering.pdf, https://dev.epicgames.com/documentation/unreal-engine/exponential-height-fog-in-unreal-engine?lang=en-US, https://blog.durablescope.com/post/WaterSimCaustics/, http://www.cemyuksel.com/research/heightfield_caustics/fastcaustics.pdf, https://www.gamedeveloper.com/programming/inexpensive-underwater-caustics-using-cg
