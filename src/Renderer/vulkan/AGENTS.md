# Vulkan Rendering Backend

- run and open to a game: `zig build run -Dtest_play`

This backened should function as clostly as possible to the opengl one. 
For each change check how its done in the opengl backened and follow it.